import { DotDividedShortDatePipe } from './dot-divided-short-date.pipe';

describe('DotDividedShortDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DotDividedShortDatePipe();
    expect(pipe).toBeTruthy();
  });
});
